<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Find a Slice</title>
  </head>

  <body>
    <nav>
      <button><a href="/pizza/new">Add a Slice</a></button>
    </nav>

    <h1>How You Dooin?</h1>

    <ul>
      <% allPizzas.forEach(pizza => { %>
      <li>
        <a href="/pizza/<%=pizza.name; %>"> <%=pizza.name; %> </a>

        <form
          style="display: inline"
          action="/pizza/<%= pizza.name %>?_method=POST"
          method="POST"
        >
          <input type="submit" value="DELETE" />
        </form>
      </li>
      <% }) %>
    </ul>
    <!-- <form id="search">
      <input type="text" placeholder="Name of Shop or Borough" />
      <input type="submit" value="Lets Go" />
    </form> -->
  </body>
</html>
